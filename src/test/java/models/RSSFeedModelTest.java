package models;

import org.junit.Test;

import static org.junit.Assert.*;

public class RSSFeedModelTest
{
    @Test
    public void makingInstanceAndCheckGetters()
    {
//        RSSFeedModel rssFeedModel = new RSSFeedModel("title", "link", "description",
//                "language", "copyRight", "1998.3.3");
//
//        assertEquals("title", rssFeedModel.getTitle());
//        assertEquals("link", rssFeedModel.getLink());
//        assertEquals("description", rssFeedModel.getDescription());
//        assertEquals("copyRight", rssFeedModel.getCopyright());
//        assertEquals("1998.3.3", rssFeedModel.getPubDate());
    }

}