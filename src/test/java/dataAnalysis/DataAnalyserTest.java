package dataAnalysis;

import core.Core;
import org.junit.Test;
import webSiteRepository.WebSiteRepository;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class DataAnalyserTest {

  WebSiteRepository webSiteRepository = mock(WebSiteRepository.class);
  @Test
  public void getLastDayStatics() {
//    for (int i = 0; i < 10; i++) {
//      System.out.println(
//          Core.getInstance().getDateEngine().getNewsCountForDay("https://www.isna.ir/rss", i));
//    }
  }
}
